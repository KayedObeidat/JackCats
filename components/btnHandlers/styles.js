import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    cardTouch: {
        width: '100%',
        height: '100%',
        alignItems: 'center',
        justifyContent: 'center',
        borderRadius: 50,
        position: 'relative',
        flexDirection: 'row',

    },
});

export {styles};
