import { StyleSheet } from "react-native";


const styles = StyleSheet.create({
    pickerStyle: {
        width: '100%',
        justifyContent: 'center',
        alignItems: 'center',
    },
    pickerItemStyle: {
        backgroundColor: '#fff',
    },
    container: {
        width: '100%',
        height: 60,
        borderWidth: 1,
        borderColor: '#9e9b9e',
        borderRadius: 5,
        marginBottom: 5,
        justifyContent: 'center',
    }
})

export { styles }